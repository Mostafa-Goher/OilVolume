using IOC;
using Math.LinearAlgebra;
using Ninject;
using System.IO.Abstractions;
using VolumeCalculation;

namespace MatricesTest
{
    class Program
    {
        
        static void Main(string[] args)
        {/*
            var volumeCalculator = NinjectKernel.CreateKernel().Get<IVolumeCalculator>();
            var topMatrix = volumeCalculator.Import(@"C:\file.txt");
            var matrix = volumeCalculator.GetMatrixTobeCaluclated(topMatrix);
            var volumeFeets = volumeCalculator.CalculateVolume(matrix);*/
        }
    }
}
